node_modules/
.DS_Store
*.log
.env
.env.*

# Restos de locks de git creados desde un mount sin permiso de borrado.
# Se puede borrar la carpeta a mano sin ningún riesgo.
_git-basura/
.test-backup/

# Material de investigación temporario del modo --research (scripts/lib/research.js).
# Nunca se commitea: es insumo de la corrida, no dato publicado.
.radar-research/
